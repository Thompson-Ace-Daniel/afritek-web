# Dependencies
node_modules/

# Build output
dist/
build/

# Environment variables
# `.env*` rather than a list: `.env.production` was slipping through `.env` +
# `.env.local` + `.env*.local` and would have been committed with its API keys.
.env*

# Logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# macOS
.DS_Store

# IDE
.vscode/
.idea/
.vercel
